3
// patterns case Cons(x, Cons(y, Cons(3, Cons(4, _)))) and case Cons(h, t) both
// match the expression, but the case Cons(x, Cons(y, Cons(3, Cons(4, _))) comes
// first in terms of the order of the patterns
